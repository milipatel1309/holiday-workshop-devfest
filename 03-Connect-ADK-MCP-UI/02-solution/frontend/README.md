# Workshop Step 3: Connect ADK Agent to MCP UI (Solution)

This is the **Completed Solution** for the "Connect ADK Agent to MCP UI" workshop step.

It demonstrates a fully connected React frontend talking to the Python ADK Agent.

## 🚀 How to Run

1.  **Install Dependencies:**
    ```bash
    npm install
    ```

2.  **Run Development Server:**
    ```bash
    npm run dev
    ```

3.  **Open in Browser:**
    Navigate to `http://localhost:3010`.

## ✨ Key Features

-   **3D Interactive Tree**: Manipulate the tree with hand gestures or mouse.
-   **Chat Interface**: Talk to the Christmas Tree Agent directly from the UI.
-   **Backend Connection**: Real-time communication with the MCP Agent.
